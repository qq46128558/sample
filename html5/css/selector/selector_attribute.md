## 属性选择器

~~~
E[a] {属性}
匹配拥有a属性的所有E元素,不论a属性的值为何.

E[a="v"] {属性}
匹配拥有a属性, 且a属性的值为v的所有E元素.

E[a~="v"] {属性}
匹配拥有a属性, 且a属性以空白分隔的值列表中有一个值为v的所有E元素.

E[a|="v"] {属性}
匹配拥有a属性, 且a属性以连字符分隔的值列表中, 第一个值是v的所有E元素.
~~~
